<div class="row mb-4 project border border-dark post">
  <div class="col-md-5 post-header d-flex justify-content-between">
    <h2>      
      <a href="/post/{{id}}">{{title}}</a>
    </h2>
   <h3>
       Posted by {{user.username}} on {{format_date date_created}}
   </h3>
  </div>
  <div class="col-md-7 post-body">
    <p>
      {{content}}
    </p>
  </div>
</div>